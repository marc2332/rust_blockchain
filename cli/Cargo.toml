[package]
name = "cli"
version = "0.1.0"
edition = "2021"

[dependencies]
node = { path = "../node"}
blockchain = {path = "../blockchain"}
tokio = { version = "1.9.0", features = ["rt-multi-thread", "macros"]}
futures = "0.3.16"